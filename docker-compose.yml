services:
  backend:
    build: ./backend
    container_name: guardianbox-backend

    ports:
      - "4000:4000"

    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: guardianbox-frontend

    ports:
      - "8080:80"

    depends_on:
      - backend

    restart: unless-stopped

